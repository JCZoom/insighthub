/**
 * Freshdesk — public surface (stub).
 *
 * Real implementation lands in the next commit. The stub keeps the barrel
 * import in `freshworks/index.ts` resolvable so the rest of the suite
 * builds while Freshdesk is being added.
 */
export {};
